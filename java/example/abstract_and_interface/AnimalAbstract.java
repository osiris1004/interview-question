package abstract_and_interface;

public abstract class AnimalAbstract {
    int age;
    String name;
    public abstract void makeNoise();
    
}

/* !!
!! an abstract class enforce and organize exactly every subclass has to have


!! 1 Can have abstract methods (methods without a body) and concrete methods (with a body).

!! 2 Can have instance variables, constants, and static variables.

!! 3 Can have access modifiers for methods and fields (public, private, protected, default).

!! 4 Supports single inheritance; a class can extend only one abstract class.

!!  Can't have constructors hence can not be initialize.









*/